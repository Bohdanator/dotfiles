[terminal]
    vt = 1

[default_session]
    command = "agreety --cmd 'sway --unsupported-gpu 2> ~/sway-log'"
    user = "greeter"

[initial_session]
    command = "sway --unsupported-gpu 2> ~/sway-log"
    user = "{{ .chezmoi.username }}"
